{# GenyBundle:Builder:fields.html.twig #}

{% for field in entity.fields %}
    <div id="field-{{ entity.id }}-{{ field.id }}">
        {{ render(controller('GenyBundle:Builder:field', {formId: entity.id, fieldId: field.id})) }}
    </div>
{% endfor %}
