{# GenyBundle:Builder:fieldValidation.html.twig #}

<button class="btn btn-default" data-toggle="modal" data-target="#validation-modal-{{ fieldId }}">
    <span class="glyphicon glyphicon-ok"></span> {{ 'geny.view.field_validation.button' | trans({}, 'geny') }}
</button>

{% import 'GenyBundle::macros.html.twig' as macros %}

{% set body %}

    <div class="text-left">

        <p>blah blah</p>

    </div>

{% endset %}

{% set footer %}

{% endset %}

{{
    macros.modal(
        'validation-modal-' ~ fieldId,
        'geny.view.field_validation.title' | trans({'%name%': entity.name}, 'geny'),
        body,
        footer
    )
}}
