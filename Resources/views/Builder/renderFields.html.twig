{# GenyBundle:Builder:renderFields.html.twig #}

{% for field in entity.fields %}

    {{ render(controller('GenyBundle:Builder:fieldEditor', {form_id: entity.id, field_id: field.id})) }}

{% endfor %}
