{# GenyBundle:Builder:field.html.twig #}

<div id="geny-field-settings-{{ fieldId }}" style="margin-bottom: 3px;" class="pull-right geny-script-display-settings">
    <button class="btn btn-sm btn-default geny-script-display-button" data-geny-target="#geny-field-{{ fieldId }}">
        <span class="glyphicon glyphicon-wrench" aria-hidden="true"></span>
    </button>
</div>

<div
    id="geny-field-preview-{{ fieldId }}" class="geny-script-preview">
    {{ render(controller('GenyBundle:Builder:fieldPreview', {formId: formId, fieldId: fieldId})) }}
</div>

<div id="geny-field-details-{{ fieldId }}" class="geny-script-display-handle hide">
    {{ render(controller('GenyBundle:Builder:fieldDetails', {formId: formId, fieldId: fieldId})) }}
</div>
