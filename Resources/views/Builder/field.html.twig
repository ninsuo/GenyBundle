{# GenyBundle:Builder:field.html.twig #}

{% set form_id = entity.form.id %}
{% set field_id = entity.id %}

<div
    id="geny-field-readonly-{{ field_id }}" class="geny-script-readonly">
    {{ render(controller('GenyBundle:Builder:fieldPreview', {formId: form_id, fieldId: field_id})) }}
</div>

<div id="geny-field-actions-{{ field_id }}">
    {# default|options|validators|delete|type #}
</div>

<div id="geny-field-details-{{ field_id }}" class="geny-script-field-handle hide">
    {{ render(controller('GenyBundle:Builder:fieldDetails', {formId: form_id, fieldId: field_id})) }}
</div>


{#

- when adding a new field, automatically focus on the last one added
- when validating rendered form, display resulting array
- put a nice stuff when hovering or when opening a field
- display all pane buttons (without actions at the moment)
- put all required scripts (domajax, ... except jquery/bootstrap) in demo view


    name, label, hint, readonly
    default value
    options
    validators
    remove
    type
    move up / down
    add

#}